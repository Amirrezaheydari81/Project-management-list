{
  "name": "project-management-list",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tailwindcss -i src/css/input.css -o src/style.css",
    "watch": "tailwindcss -i src/css/input.css -o src/style.css --watch",
    "minify": "tailwindcss -i src/css/input.css -o src/style.min.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-plugin-tailwindcss": "^0.3.0",
    "tailwindcss": "^3.3.2"
  }
}
